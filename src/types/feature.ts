export interface FeatureType {
  id: string;
  image: string;
  name: string;
  price: number;
  type: string;
  description: string;
}

export interface VehicleType {
  id: string;
  name: string;
}
